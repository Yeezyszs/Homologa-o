import { EmConstrucao } from '@/ui/components/EmConstrucao'

// Milestone 5 — cadastro/edição + vínculo de segmentos + rótulo de risco.
export function FornecedorForm() {
  return <EmConstrucao titulo="Cadastro de fornecedor" milestone="milestone 5 (fornecedores)" />
}
