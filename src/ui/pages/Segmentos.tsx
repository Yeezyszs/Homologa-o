import { EmConstrucao } from '@/ui/components/EmConstrucao'

// Milestone 4 — CRUD de segmentos + montagem do checklist.
export function Segmentos() {
  return <EmConstrucao titulo="Segmentos" milestone="milestone 4 (catálogo)" />
}
