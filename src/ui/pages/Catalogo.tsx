import { EmConstrucao } from '@/ui/components/EmConstrucao'

// Milestone 4 — CRUD de tipos_documento (tem_validade, origem).
export function Catalogo() {
  return <EmConstrucao titulo="Catálogo de documentos" milestone="milestone 4 (catálogo)" />
}
