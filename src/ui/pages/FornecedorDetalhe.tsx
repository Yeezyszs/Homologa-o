import { EmConstrucao } from '@/ui/components/EmConstrucao'

// Milestone 6 — dados, segmentos, checklist, upload/substituir e histórico.
export function FornecedorDetalhe() {
  return <EmConstrucao titulo="Detalhe do fornecedor" milestone="milestone 6 (documentos)" />
}
